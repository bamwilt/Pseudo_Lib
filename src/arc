//MÉTODO                               | PARÁMETROS            | DEVUELVE        | DESCRIPCIÓN BREVE
//----------------------------------------------------------------------------------------------------------------------------
//adjustInto(Temporal temporal)        | Ambas                 | Temporal        | Ajusta el objeto recibido para que tenga esta fecha.
//atStartOfDay(ZoneId zone)            | Ambas                 | ZonedDateTime   | Crea la fecha-hora con zona al inicio del día.
//atTime(OffsetTime time)              | Ambas                 | OffsetDateTime  | Combina la fecha con una hora con zona/offset.

//compareTo(ChronoLocalDate other)     | Ambas                 | int             | Compara fechas (devuelve 0 si son iguales).

//equals(Object obj)                   | Ambas                 | boolean         | Verifica si el objeto es idéntico a la fecha.
//format(DateTimeFormatter f)          | Ambas                 | String          | Convierte la fecha a texto según un formato.
//from(TemporalAccessor t)             | Explícito (Estático)  | LocalDate       | Crea un LocalDate desde otro objeto temporal.

//getChronology()                      | Implícito             | IsoChronology   | Obtiene el sistema de calendario (ISO).

//getLong(TemporalField field)         | Ambas                 | long            | Obtiene un valor específico como long.

//isSupported(TemporalField field)     | Ambas                 | boolean         | ¿Acepta este campo de tiempo?
//isSupported(TemporalUnit unit)       | Ambas                 | boolean         | ¿Acepta esta unidad de tiempo (días, meses)?
//now(Clock clock)                     | Explícito (Estático)  | LocalDate       | Obtiene la fecha actual desde un reloj específico.
//now(ZoneId zone)                     | Explícito (Estático)  | LocalDate       | Obtiene la fecha actual en una zona horaria.

//parse(CharSequence text)             | Explícito (Estático)  | LocalDate       | Lee un texto "AAAA-MM-DD" y lo hace fecha.
//parse(CharSequence t, DTFormatter f) | Explícito (Estático)  | LocalDate       | Lee un texto usando un formato personalizado.
//query(TemporalQuery<R> query)        | Ambas                 | R               | Realiza una consulta personalizada a la fecha.
//range(TemporalField field)           | Ambas                 | ValueRange      | Obtiene el rango de valores (ej. meses del 1-12).

//until(ChronoLocalDate end)           | Ambas                 | Period          | Calcula tiempo faltante en años, meses, días.
//until(Temporal end, TemporalUnit u)  | Ambas                 | long            | Calcula tiempo faltante en una unidad (ej. días).

//with(TemporalAdjuster adjuster)      | Ambas                 | LocalDate       | Ajusta la fecha (ej. "primer lunes del mes").
//=====[ CONSTANTS/DEFINITIONS ]=====[  ///   ]=========================================
Algoritmo asd
FinAlgoritmo


	